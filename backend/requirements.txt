# 核心（轻量、秒装）——行情 + 研报即刻可用
fastapi>=0.110
uvicorn[standard]>=0.29
requests

# 全栈数据源（较重）——一致预期 / 新闻 / 公告 / K线 / 财务
# 未安装时对应端点会返回 501 + 安装提示，不影响行情/研报
akshare>=1.10
mootdx>=0.10
pandas

# 用户数据持久化与版本化迁移（PostgreSQL）
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<1.17
psycopg[binary]>=3.2,<3.3
argon2-cffi>=23.1,<26
cryptography>=43,<46
redis>=5,<7
prometheus-client>=0.21,<1
